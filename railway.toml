[build]
builder = "nixpacks"
buildCommand = "npm install"
aptPkgs = ["python3", "python3-pip", "build-essential", "libsqlite3-dev"]

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"

[service]
name = "recruitment-system"

[service.healthcheck]
path = "/api/health"
